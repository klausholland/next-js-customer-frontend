## Customer Manager

Frontend application for managing customers and their address data.

### Setup

To build and run the application run the following command:

```bash
docker compose up -d
```

The application can be accessed in the browser on `http://localhost:3000`